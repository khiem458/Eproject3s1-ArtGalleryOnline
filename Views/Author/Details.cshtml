@model ArtGalleryOnline.Models.AuthorArtWork

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="card mb-4">
    <div class="card-body">
        <h1 class="card-title text-center">Details</h1>

        <div>
            <h4 class="card-title text-center">AuthorArtWork</h4>
            <hr />
            <dl class="row justify-content-center">
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Artist)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Artist)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.AuthDesciption)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.AuthDesciption)
                </dd>
            </dl>
        </div>
        <div>
            <a asp-action="Edit" asp-route-id="@Model?.AuthId" class="btn btn-primary">Edit</a> |
            <a asp-action="Index" class="btn btn-secondary">Back to List</a>
        </div>
    </div>
</div>
